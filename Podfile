# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Googlemap22' do
pod 'GooglePlaces'
  pod 'GooglePlacePicker'
  pod 'GoogleMaps'
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Googlemap22

  target 'Googlemap22Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Googlemap22UITests' do
    # Pods for testing
  end

end
